{
    "/api": {
      "target": "https://api.lawyercloud.work.gd",   // Your backend API server
      "secure": true,  // Use 'true' if your API is HTTPS
      "changeOrigin": true,  // Change the origin to match the target
      "logLevel": "debug",  // Useful for debugging requests
      "pathRewrite": {
        "^/api": ""  // Optionally rewrite path if the backend does not need "/api" prefix
      }
    }

}
  